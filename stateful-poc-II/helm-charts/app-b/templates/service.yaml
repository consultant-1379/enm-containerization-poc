apiVersion: v1
kind: Service
metadata:
  name: app-b
  labels:
    app: app-b
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-b
