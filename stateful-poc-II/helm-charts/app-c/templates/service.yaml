apiVersion: v1
kind: Service
metadata:
  name: app-c
  labels:
    app: app-c
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-c
