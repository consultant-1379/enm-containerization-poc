apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smrs
  annotations:
   volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
