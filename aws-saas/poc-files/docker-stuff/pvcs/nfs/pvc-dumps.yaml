apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hcdumps
  annotations:
   volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 0.5Gi
