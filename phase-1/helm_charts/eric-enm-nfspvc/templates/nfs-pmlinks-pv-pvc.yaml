apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pmlinks
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.pmlinksSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
