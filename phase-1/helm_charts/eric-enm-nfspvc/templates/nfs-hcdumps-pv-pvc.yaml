apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-hcdumps
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.hcdumpsSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
