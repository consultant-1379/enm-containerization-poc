apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-amos
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.amosSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
