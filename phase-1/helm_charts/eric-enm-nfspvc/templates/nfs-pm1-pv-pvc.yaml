apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pm1
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.pm1Size }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
