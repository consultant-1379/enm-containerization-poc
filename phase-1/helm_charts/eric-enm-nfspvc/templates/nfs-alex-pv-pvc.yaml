apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-alex
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.alexSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
