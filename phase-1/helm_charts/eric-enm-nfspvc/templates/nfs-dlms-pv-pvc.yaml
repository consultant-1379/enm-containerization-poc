apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-dlms
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.dlmsSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
