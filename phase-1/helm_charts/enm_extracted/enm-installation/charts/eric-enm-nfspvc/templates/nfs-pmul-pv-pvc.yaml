apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pmul
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.pmulSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
