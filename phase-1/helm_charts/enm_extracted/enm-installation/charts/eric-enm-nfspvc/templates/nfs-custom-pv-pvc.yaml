apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-custom
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.customSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
