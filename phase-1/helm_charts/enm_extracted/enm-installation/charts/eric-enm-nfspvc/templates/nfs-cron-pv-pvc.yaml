apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-cron
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.cronSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
