apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-sdn
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.sdnSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
