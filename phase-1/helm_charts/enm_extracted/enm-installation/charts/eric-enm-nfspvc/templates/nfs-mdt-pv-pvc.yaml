apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-mdt
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.mdtSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
