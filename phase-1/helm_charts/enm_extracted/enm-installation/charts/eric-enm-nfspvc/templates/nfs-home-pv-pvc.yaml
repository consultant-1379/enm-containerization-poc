apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.nfs.homeSize }}
  storageClassName: {{ .Values.global.nfs.storageClass | quote }}
