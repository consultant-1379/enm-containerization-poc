apiVersion: apps/v1
kind: Deployment
metadata:
  name: gp
spec:
  selector:
    matchLabels:
      app: gp
  replicas: 1
  template:
    metadata:
      labels:
        app: gp
    spec:
#      initContainers:
      containers:
      - name: gp
        #image: armdocker.rnd.ericsson.se/proj-enmk8spoc/neo-cypher:0.0.4
        image: 152254703525.dkr.ecr.eu-west-1.amazonaws.com/eirepoc1-registry:cyphershell-alpine
        imagePullPolicy: Always
        command: ["/bin/sh", "-c"]
        args:
#          - bash configure.sh;
          - ln -s /gp/global.properties /ericsson/tor/data/global.properties.mick;while true; do sleep 300;done;
        volumeMounts:
          - name: pv-gp
            mountPath: /gp
          - name: pv-data
            mountPath: /ericsson/tor/data
      volumes:
        - name: pv-gp
          configMap:
            name: gpcloud1
        - name: pv-data
          persistentVolumeClaim:
            claimName: data
