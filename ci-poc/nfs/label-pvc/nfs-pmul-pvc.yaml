apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pmul
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      enmnfs: nfs-pmul
  storageClassName: ""
