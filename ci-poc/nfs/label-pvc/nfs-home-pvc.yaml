apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      enmnfs: nfs-home
  storageClassName: ""
