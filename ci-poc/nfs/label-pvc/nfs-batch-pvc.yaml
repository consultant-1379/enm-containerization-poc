apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-batch
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  selector:
    matchLabels:
      enmnfs: nfs-batch
  storageClassName: ""
