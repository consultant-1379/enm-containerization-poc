apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pmlinks
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      enmnfs: nfs-pmlinks
  storageClassName: ""
