apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-configmgt
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      enmnfs: nfs-configmgt
  storageClassName: ""
